# SchoolCircle artifact

The SchoolCircle application is served by the root Next.js app in [`../../app`](../../app).
Run the repository's root scripts for development and production; this artifact directory is
retained only for artifact metadata and static references.

This package intentionally has no application runtime, Vite configuration, dependencies, or
build scripts.
