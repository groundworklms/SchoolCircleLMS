{
  "name": "schoolcircle-lms",
  "version": "0.1.0",
  "private": true,
  "description": "SchoolCircle — grounded, offline, human-led LMS. Every answer cites the manual or refuses.",
  "scripts": {
    "dev": "next dev -p 3111",
    "build": "next build",
    "start": "next start -p 3111",
    "lint": "next lint",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:seed": "node prisma/seed.js",
    "postinstall": "prisma generate"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "next": "15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "prisma": "^6.1.0"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
